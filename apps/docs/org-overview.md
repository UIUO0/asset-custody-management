# تخصيص جهة حكومية — نقطة البداية

هذا المستودع نسخة من Shelf.nu مُخصّصة لنشر داخلي في **هيئة تطوير المنطقة
الشرقية**. توجد سبعة مستندات `org-*` ولم يكن بينها خريطة — هذا الملف هو
الخريطة. **ابدأ من هنا** قبل أي عمل على التخصيص.

---

## اقرأ بهذا الترتيب

| #   | المستند                                                                      | متى تحتاجه                                                                                                                      |
| --- | ---------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| ١   | [`org-workflow-and-roles.md`](./org-workflow-and-roles.md)                   | **المواصفة المرجعية.** سير العمل المطلوب في الهيئة، الأدوار، المراحل، القرارات المعتمدة. أي خلاف حول «ماذا يُبنى» يُحسم من هنا. |
| ٢   | [`org-i18n-and-theming.md`](./org-i18n-and-theming.md)                       | **إلزامي قبل أي تعديل على الواجهة.** قواعد RTL والخصائص المنطقية وإضافة النصوص المترجمة.                                        |
| ٣   | [`org-glossary.md`](./org-glossary.md)                                       | **إلزامي قبل كتابة أي نص عربي.** المصطلحات معتمدة من مالك المشروع؛ لا تجتهد بترجمة بديلة.                                       |
| ٤   | [`org-phase1-role-check-inventory.md`](./org-phase1-role-check-inventory.md) | خلفية أمنية: كيف حُوّلت ١٠٨ فحوصات أدوار سلبية إلى قائمة سماح مركزية.                                                           |
| ٥   | [`org-asset-intake-workflow.md`](./org-asset-intake-workflow.md)             | مسار استلام الصنف (`PENDING` → `READY`).                                                                                        |
| ٦   | [`org-booking-request-workflow.md`](./org-booking-request-workflow.md)       | مسار طلبات الموظفين والتعليق للمراجعة.                                                                                          |
| ٧   | [`org-i18n-progress.md`](./org-i18n-progress.md)                             | سجل تقدّم التعريب — ما تُرجم وما تبقّى.                                                                                         |
| ٨   | [`org-azure-entra-sso.md`](./org-azure-entra-sso.md)                         | تفعيل الدخول الموحّد لاحقاً (إعدادات فقط، بلا كود).                                                                             |

`CLAUDE.md` في جذر المستودع يلخّص القواعد الحاكمة والمزالق الهندسية — يُقرأ
تلقائياً، لكنه ملخّص لا بديل عن المستندات أعلاه.

---

## أين وصل التنفيذ

| المرحلة                     | الحالة                                                                                                  |
| --------------------------- | ------------------------------------------------------------------------------------------------------- |
| ١ — أساس الأدوار والصلاحيات | ✅ مكتملة ومشتغَّلة                                                                                     |
| ٢ — دورة حياة الصنف         | 🟡 **٣ من ٦** — محور الحالة (٦، ٩، ١٠) مكتمل؛ محور المالية (٧، ٧ب، ٨) لم يُلمس                          |
| ٣ — الطلب والموافقة         | 🟡 **٢٫٥ من ٥** — الطلب والقبول والرفض والتعليق مبنية؛ **التوقيع (١٤، ١٥) لم يُبنَ** ← الأولوية التالية |
| ٤ — الإرجاع ومآلاته         | ⬜ لم تبدأ                                                                                              |
| ٥ — التقارير والمراقبة      | ⬜ لم تبدأ                                                                                              |
| ٦ — استئناف التعريب         | 🟡 مستمرة بالتوازي                                                                                      |

### ما هو مبني فعلاً وشُغِّل

- الأدوار الثلاثة (`WAREHOUSE` / `FINANCE` / `INVENTORY`) في المخطط وخريطة الصلاحيات.
- إجراءا `approve` و `hold`، وقائمة السماح المركزية `role-scope.ts`.
- مسار استلام الصنف كاملاً: الحقل، الاعتماد الفردي والجماعي، الإرجاع للمراجعة
  بسبب إلزامي، حجب `PENDING` عن الموظف على مستوى الاستعلام في كل مسارات القراءة
  والكتابة، وعمود وفلتر في الفهرس المتقدّم.
- مسار الطلبات: `approvalState`، صفحة `/requests`، القبول والرفض والتعليق،
  وبوابة في `reserveBooking` تمنع حجز ما لم يُقبل.
- صفحتا الموظف: `/available-assets` و `/my-custody`.
- إعادة تقسيم القائمة الجانبية حسب الدور.

### هل خولف ترتيب المراحل؟

سؤال يتكرّر عند مراجعة التقدّم، فالجواب هنا مرة واحدة.

المستند الأصلي قال «كل مرحلة تعتمد على ما قبلها»، وهذه صياغة فضفاضة. الواقع:

- **المرحلة ٢ ليست متروكة.** محورها الأول — حالة الصنف `PENDING → READY` —
  مبني بالكامل ومشتغَّل. المتروك هو محورها الثاني: الحقول المالية.
- **بناء الطلبات (١٢، ١٣) لم يقفز فوق اعتمادية.** الطلب يعمل على
  `Booking.approvalState`، والحقول المالية على `Asset` — لا تقاطع تقنياً.
- **الاعتمادية المكسورة الوحيدة داخل المرحلة ٣ نفسها:** القبول مبني والتوقيع
  لا، فالتسليم يجري بلا توقيع.

جدول الاعتماديات الفعلية في
[org-workflow-and-roles.md](./org-workflow-and-roles.md) — راجعه قبل إعادة
ترتيب أي عمل بناءً على أرقام المراحل وحدها.

### الفجوات المعروفة

- **لا توقيع عند التسليم** — بند أساسي في المواصفة (المرحلة ٣).
- **الموظف لا يرى مصير طلبه** إلا عبر ملاحظات الحجز؛ لا صفحة «طلباتي».
- **«الأصناف المتاحة» تعرض الأصناف فقط لا المجموعات** — طلب مجموعة يمرّ عبر
  نموذج الحجز.
- **حقول المالية غير مبنية**: لا `purchaseDate`، ولا حقل ترميز، ولا تاريخ
  انتهاء إهلاك، ولا حصر للكتابة عليها بدور المالية (المرحلة ٢: ٧، ٧ب، ٨).
  ظهور `lifecycleStage` في تصدير CSV عمودٌ ورثه المُصدِّر تلقائياً، وليس
  تنفيذاً للبند ٨.
- الاعتماد الجماعي في اتجاه الإفراج فقط؛ الإرجاع للمراجعة فردي عمداً.

---

## القواعد الحاكمة

هذه ليست تفضيلات أسلوبية — كسرُها يُنتج أعطالاً أمنية أو يكسر البناء.

**١. لا فحص سلبي للأدوار.** `role !== SELF_SERVICE` يعني عملياً «كل دور يُضاف
لاحقاً مسؤول». النطاق من `rolesAreScopedToOwnRecords`، والصلاحية من
`userHasPermission`. الدور غير المسجَّل يُحجب افتراضياً.

**٢. النطاق ≠ الصلاحية.** «أي الصفوف يراها» سؤال مستقل عن «أي إجراء يملكه».
استخدام أحدهما مكان الآخر يكسر فلترة الصفوف أو يفتح إجراءً.

**٣. حالة سير العمل حقل مستقل، لا قيمة جديدة في enum قائم.** `AssetStatus` و
`BookingStatus` يُفحصان `switch` فحصاً شاملاً في عشرات المواضع؛ توسيعهما يغيّر
كل واحد منها بصمت.

**٤. المنع في الاستعلام وطبقة الخدمة، لا في الواجهة.** إخفاء الزر لطف، لا حاجز.
والفلترة في الاستعلام تُبقي العدّادات والترقيم صادقَين.

**٥. كل نص ظاهر يمرّ بـ `t()` ويُضاف إلى `ar.json` و `en.json` معاً.** الفرق
الوحيد المسموح بين الملفين هو صيغ الجمع العربية (`_zero` / `_two` / `_few` /
`_many`).

**٦. ممنوع أي ذكر لـ `shelf` أو `shelf.nu` في أي سطح يراه المستخدم.**

**٧. لا `git add` ولا `git commit` تلقائياً** — إلا بطلب صريح.

---

## قبل أن تُسلّم عملك

```bash
pnpm db:deploy-migration     # إن مسست المخطط
rm -rf apps/webapp/node_modules/.vite   # بعد أي تغيير في المخطط
pnpm webapp:validate         # اختبارات + lint + أنواع
```

وتأكّد من:

- تطابق مفاتيح `ar.json` و `en.json` (عدا صيغ الجمع العربية).
- أن أي مسار جديد أُضيف إلى القائمة الجانبية بشرط ظهور صحيح، وأن عنوان القسم
  يحمل نفس الشرط.
- أن أي قاعدة أمنية جديدة يحرسها اختبار — لا تعليق في الكود.
- تحديث المستند المعني في `apps/docs/` وهذا الجدول أعلاه.
